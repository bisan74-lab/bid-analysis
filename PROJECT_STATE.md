# 프로젝트 현황 종합 문서 (AI 인수인계용)

**마지막 갱신**: 2026-07-20 (완전 클라우드 자동 갱신 + 신규 입찰 카카오 알림까지 배포·검증 완료 — 5.7장)
**작성 목적**: 이 문서 하나만 읽어도(PROGRESS.md의 세션별 기록을 다 훑지 않아도) 이 프로젝트가 무엇이고, 지금 어디까지 됐고, 무엇이 남았는지 판단할 수 있도록 작성한 종합 스냅샷. 세부 변경 이력/시행착오는 `PROGRESS.md`에, 짧은 운영 메모는 `CLAUDE.md`에 있음 — 이 문서는 "현재 상태"를 한 번에 보여주는 것이 목적.

---

## 1. 프로젝트 목적

**(주)케이알에스건설**(부산 소재, 사업자등록번호 515-81-31362, 대표 박지선)이 보유한 면허(지반조성포장/토공, 상하수도설비) 기준으로, 공공 토목공사 입찰에서 **낙찰 확률이 높은 입찰가**를 과거 낙찰 데이터 기반으로 추정하는 웹 대시보드를 만드는 프로젝트. 최종 목표: "이번 공고에 얼마로 투찰해야 낙찰 확률이 높은가"에 데이터로 답하는 것.

회사 핵심 지표(신용등급 C, 부채비율 52.06%, 시공능력평가액 등)는 `data/company-profile.md` 참고.

---

## 2. 데이터 파이프라인

### 2.1 소스와 수집 방법
- 소스: **아이건설넷(igunsul.net)** — 로그인 필요한 사이트라 일반 크롤링(WebFetch) 불가.
- 최초 데이터 수집은 Claude Code 대화 세션 안에서 **Playwright MCP**로 직접 브라우저 조작해서 진행(로그인 → 조건검색 폼 조작 → 페이지네이션 순회 → 테이블 파싱). 로그인 정보는 프로젝트 루트 `credentials.local.md`(git 미포함).
- **수집 범위**: 부산 + 경남 지역(회사 실제 입찰권), 종목 = 지반조성.포장(코드 B001) + 상.하수도(코드 B008), 최근 3년(2023-07-04 ~ 2026-07-03).
- 최종 원자료: `data/raw/nbid_busan_gyeongnam_3y_20260704.csv` — 컬럼: `posting_id, 공사명, 종목, 대업종, 발주처, 지역약칭, 기초금액, 추정가격, 예정가격, 예정가격사정률, 1순위금액, 1순위사정률, 1순위업체, 참여업체수, 개찰일, 검색지역, 연차구분`.
  - `posting_id`는 공사명 안 대괄호 코드(예: `[R26BK01608011-000]`)에서 추출한 실제 고유 ID. **주의**: 원본 사이트의 "번호" 컬�럼은 검색결과 내 순번일 뿐 실제 ID가 아니므로 사용하지 않음(이 실수를 초기에 했다가 발견/수정함).
  - `연차구분`: 최근1년 / 1~2년전 / 2~3년전 — 가중치 모델에서 사용(아래 3.1 참고).

### 2.2 관내(경남 시/군) 제한 필터 — 중요, 버그 히스토리 있음
부산 소재 회사는 **경남 산하 시/군(기초자치단체) 발주처의 관내 제한 공고에는 입찰 자격이 없다**는 사용자 지적으로 도입. 로직은 `webapp/lib/localFilter.js`에 있고, **`analysis.js`(과거 이력 로딩)와 `scraper.js`(진행중 입찰/맞춤정보 스크래핑) 양쪽에 동일 적용**됨.

- **최종 방식(default-exclude, whitelist-rescue)**: 발주처명에 경남 18개 시/군명(창원·진주·통영·사천·김해·밀양·거제·양산·의령·함안·창녕·고성·남해·하동·산청·함양·거창·합천) 중 하나라도 포함되면 **기본적으로 제외**. 단, 다음 화이트리스트 패턴에 매칭되면 국가/도(광역) 단위 기관으로 보고 **유지**: 한국OO공사류 공기업, "도로관리사업소"/"항만관리사업소", "교육청"/"교육지원청"(도교육청 산하), "소방서"/"소방본부", 군부대(문자열 **시작 위치**로 한정: `^(국방부|국군|육군|해군|공군)`), 중앙정부 부처/지방청, 국립대학교, "주식회사"(민간기업).
- **발견된 버그 2건 (교훈으로 남김 — 향후 유사 패턴 추가 시 반드시 재확인)**:
  1. 처음엔 "경남/경상남도 바로 뒤에 시/군명+시/군접미사"만 매칭하는 좁은 규칙을 썼는데, "경상남도교육청 경상남도진주교육지원청"이나 개별 학교명("삼현여자고등학교") 같은 다른 표기 형태를 놓침 → default-exclude 방식으로 전환.
  2. **더 심각한 버그**: 군부대 화이트리스트 패턴에 `해군`(Navy)을 앵커 없이 넣었더니, **"남해군"(Namhae County, 지자체)의 끝 두 글자와 우연히 매칭**돼서 남해군청 발주 건 97건이 전부 잘못 유지됐었음. `산청`이 `해양수산청`에 우연히 포함되는 것과 같은 종류의 "지명 문자열 부분매칭 오탐"으로, 반드시 문자열 **시작 위치**로 앵커링해서 회피함.
- **적용 후 최종 건수** (2026-07-05 기준): 총 **4,196건** (부산 2,882 + 경남 1,314). 원본 CSV(9,666건)에서 관내 제외로 크게 줄어든 것.

---

## 3. 통계/추천 모델 (`webapp/lib/analysis.js`)

### 3.1 가중치 (반감기 1년 지수가중)
```
최근1년(0~1년전):  57.1% (4/7)
1~2년전:          28.6% (2/7)
2~3년전:          14.3% (1/7)
```
최신 데이터에 더 높은 신뢰도를 두는 방식. 모든 평균/백분위수 계산에 이 가중치 적용.

### 3.2 핵심 용어 정의 (직접 계산, 원본 CSV의 "사정률"/"1순위사정률" 컬럼은 산출식이 불명확해 사용 안 함)
- **예가율** = 예정가격 ÷ 기초금액 (발주처가 만든 예정가격이 공개된 기초금액 대비 얼마나 차이나는지, 복수예비가격 추첨으로 결정되는 랜덤 변동, 보통 ±2~3% 이내)
- **낙찰률** = 1순위금액(낙찰금액) ÷ 예정가격 (낙찰자가 예정가격 대비 몇 %로 투찰해서 이겼는지)

### 3.3 진행중 공고 입찰가 추천 (`recommendBid(기초금액, 대업종)`)
1. 추정예정가격 = 기초금액 × (동일 종목군의 가중평균 예가율)
2. 확률 구간별 추천가 = 추정예정가격 × (가중 낙찰률의 100/80/65/50 백분위수)
3. **안정회귀형**: 가중평균 낙찰률(=평균 회귀 기대) ± 가중 표준오차(SEM, 유효표본수 보정) → 하한/중앙/상한 3개
4. **공격밀집형**: 낙찰률 히스토그램(0.25%p 단위, 가중치 적용)에서 최다빈도 구간의 경계 → 하한/상한 2개

### 3.4 TOP5 우수 업체 분석 (`getTopCompanies(n=5, {minCount=10})`, `predictCompanyBid(companyName, 기초금액, 대업종)`)
- 선정 기준: 낙찰 10건 이상 업체 풀에서 **(건수 순위 + 낙찰률 순위) 합산 종합점수가 낮을수록(=둘 다 상위권) 우선**. 단순 건수 1위가 아니라 "많이 이기면서도 낙찰률도 높은" 업체를 고르도록 설계(사용자가 명시적으로 요청한 기준).
- **데이터 정제 이슈**: "1순위업체" 필드가 처음엔 파싱조차 안 되고 있었음(발견 후 추가). "유찰"(입찰 실패)이 회사명처럼 집계되던 버그도 있었음 → `낙찰률 == null`인 행 제외로 해결(유찰 건은 예정가격=0, 낙찰금액=null이라 애초에 낙찰률이 계산 안 됨).
- 현재(2026-07-05) TOP5: **(주)청복건설**(22건, 89.8%) · **재흥건설 주식회사**(18건, 90.5%) · **주식회사 금강플러스**(18건, 89.7%) · **동림건업(주)**(16건, 89.7%) · **주식회사 신영토건**(14건, 89.9%).
- 업체별로 평균예정가격/평균낙찰금액/평균차액(원+%)/차액범위(최소~최대, 원+%)도 계산해서 카드에 바로 표시.
- `predictCompanyBid`: 특정 업체가 새 공고에 얼마 쓸지 예측. 예정가격 추정은 시장 전체 가중평균 예가율(업체 무관, 발주처가 정하는 값이므로), 투찰가는 **그 업체 본인의** 가중평균 낙찰률 적용. 종목별 표본 5건 미만이면 그 업체의 전체 낙찰이력으로 대체(신뢰도 "낮음" 표시).

### 3.5 참여업체수 vs 낙찰률
상관계수 약 +0.088(거의 무관~약한 양의 상관) — **경쟁이 치열해질수록(참여업체 많을수록) 낙찰률이 오히려 소폭 높아지는 경향** (1~20개 참여 시 평균 -10.75% 사정 → 21~50개 -10.89%(최저점) → ... → 400개 초과 -10.44%, U자형 패턴). 직관과 반대되는 결과.

---

## 4. 웹앱 아키텍처 (`webapp/`)

Node.js/Express 로컬 서버, `npm start` (또는 `node server.js`) → **http://localhost:4173**. 브라우저 접속 자체는 항상 이 로컬 주소로만 가능 — **공개 인터넷 배포는 아직 안 함**(2026-07-05 사용자가 "결정 보류" 상태로 남김. 방법 논의는 했음: Cloudflare Tunnel / 클라우드 VM / PaaS 비교, PROGRESS.md 참고).

### 4.1 파일 구조
```
webapp/
  server.js              Express 라우트 전부
  lib/
    analysis.js           CSV 로딩 + 통계/추천/TOP5 모델 (3장 내용)
    localFilter.js         경남 시/군 관내제한 필터 (2.2절)
    scraper.js             아이건설넷 로그인 + 스크래핑 (playwright npm, MCP와 별개)
    kakao.js               카카오 OAuth + "나에게 보내기" API
    notifyMessage.js        카카오 알림 메시지 텍스트 포맷터
  scripts/
    notify.js               예약 실행용: 새로고침 + 카카오 발송 (Task Scheduler가 호출)
    run_notify.bat          위 스크립트를 감싸는 배치파일(Task Scheduler가 실제 실행하는 대상)
  public/
    index.html, app.js, style.css   바닐라 JS + 손수 SVG 차트 대시보드
  data/
    open_bids.json          진행중 입찰 캐시 (gitignore)
    mybid_list.json          맞춤정보 캐시 (gitignore)
    kakao_token.json         카카오 OAuth 토큰 (gitignore)
  kakao.local.md            카카오 REST API 키 (gitignore)
```

### 4.2 API 엔드포인트 (server.js)
| 메서드/경로 | 설명 |
|---|---|
| `GET /api/history/stats` | 대시보드 상단 차트용 집계 통계 |
| `GET /api/open-bids` | 캐시된 "진행중 입찰" 목록 |
| `GET /api/mybid-list` | 캐시된 "맞춤정보" 목록 |
| `POST /api/open-bids/refresh` | **재로그인 후** 진행중입찰+맞춤정보 함께 재스크래핑(1분 내외) |
| `GET /api/analysis/:postingId` | 특정 공고 1건의 전체 분석(추천가+전략2종+TOP5업체예측). 진행중입찰/맞춤정보 **양쪽 캐시에서 검색**하므로 어느 리스트에서 클릭해도 동작 (원래 `/api/open-bids/:id/analysis`였으나 Cloudflare 정적 배포시 파일/디렉터리 경로 충돌 때문에 이 경로로 변경됨) |
| `GET /api/top-companies` | TOP5 업체 목록+낙찰이력 |
| `GET /oauth/kakao/start`, `/oauth/kakao/callback` | 카카오 1회성 OAuth 인가 플로우 |
| `POST /api/notify/test` | 카카오 테스트 메시지 발송 |

### 4.3 대시보드 화면 구성 (위→아래)
1. 핵심 지표 (전체 표본, 부산/경남 건수, 낙찰률 중앙값)
2. **맞춤정보 등록 항목** — 아이건설넷 계정에 저장된 검색조건 기준 자동매칭 목록(현재 33건). 클릭 시 상세분석.
3. **진행중 입찰 항목 (실시간)** — 부산/경남+지정 종목 필터링된 목록("실시간 새로고침" 버튼으로 재스크래핑). 클릭 시 상세분석.
   - **상세분석 패널 내용**(2와 3 공통): 낙찰확률 100/80/65/50% 추천가 4칸 → 안정회귀형(하한/중앙/상한) → 공격밀집형(하한/상한) → **TOP5 업체별 예측 입찰가**. 모두 한 패널에 통합됨(원래는 별도 표였는데 사용자 요청으로 통합함).
4. 입찰가 추천 방법론 설명(가중치 칩 표시)
5. 낙찰 이력 분석 차트 9종: 연차별 건수, 지역별, 업종별, 월별추이, 낙찰률분포, 금액구간별, 발주처TOP15, 시군구TOP20, 참여업체수-낙찰률(산점도+구간별 평균 추세선)
6. **TOP5 우수 업체 분석** — 각 카드에 건수/평균낙찰률/평균예정가격/평균낙찰금액/평균차액(원+%)/차액범위 표시, 클릭 시 건별 낙찰이력 상세(차액 컬럼 포함)
7. 원자료 표(접근성용, 월별 낙찰건수)

### 4.4 GitHub + Cloudflare Workers 배포 (2026-07-05 추가)
- **GitHub**: `github.com/bisan74-lab/bid-analysis` (비공개). 전체 코드+3년치 CSV 원자료 커밋됨. `credentials.local.md`/`kakao.local.md`/토큰 파일은 gitignore로 제외(과거 커밋에도 없음, 매번 push 전 재확인할 것 — 한 번은 문서(PROGRESS.md)에 실수로 카카오 키를 그대로 적었다가 커밋 직전에 발견해서 삭제한 적 있음).
- **Cloudflare Workers**: Node/Playwright를 못 돌리는 서버리스 환경이라, 앱을 그대로 올릴 수 없음. 대신 **빌드 시점 스냅샷을 정적 JSON으로 미리 생성**해서 서빙하는 방식으로 배포:
  - `webapp/data/snapshot/*.snapshot.json`: 커밋된 특정 시점 스냅샷(라이브 캐시의 사본, 최신화하려면 로컬에서 새로고침 후 이 파일들에 다시 복사→커밋→푸시).
  - `webapp/build-static.js`: 스냅샷 기준으로 모든 API 응답(통계/TOP5/공고별 상세분석 등)을 정적 파일로 만들어 `webapp/dist`에 출력(외부 npm 의존성 0개 — Playwright 설치 트리거 안 됨).
  - `wrangler.toml`(레포 루트): `[assets] directory = "./webapp/dist"`.
  - **Cloudflare 대시보드에 수동 설정 필요**(AI가 접근 불가): Build command = `node webapp/build-static.js`, Build output directory = `webapp/dist`.
  - 정적 배포본은 "실시간 새로고침"/카카오 알림이 동작 안 함 — `window.__SNAPSHOT_MODE__` 플래그로 감지해서 버튼 비활성화 + 스냅샷 시각 배너 표시.
  - API 경로 `/api/open-bids/:id/analysis`는 정적 파일-디렉터리 경로 충돌 때문에 `/api/analysis/:id`로 변경됨(로컬 서버·정적 배포 공통).

---

## 5. 카카오톡 "나에게 보내기" 자동 알림

- **연동 완료, 테스트 발송 성공 확인함.** 카카오 디벨로퍼스 앱("케이알에스 입찰알림", ID 1504038) 생성 완료, REST API 키는 `webapp/kakao.local.md`. Redirect URI는 콘솔에서 **"앱 > 플랫폼 키" 페이지의 REST API 키 카드를 클릭해서 들어간 상세화면**에 등록해야 했음(콘솔 UI가 계속 개편돼서 "카카오 로그인 > 일반"에는 없었음 — 다음에 새 앱 만들 때 참고). Client Secret은 "사용 안 함"으로 꺼서 KOE010 에러 해결.
- Windows 작업 스케줄러에 `BidAnalysis-KakaoNotify` 태스크로 매일 08:00 실행 등록(`scripts/run_notify.bat` 호출).
- **⚠️ 미해결 이슈**: 이 스케줄러 태스크가 실제로 로그 파일(`webapp/notify.log`)을 남기고 카카오톡을 정상 발송하는지 **끝까지 검증 못함** (PowerShell 안전점검 도구가 세션 중 일시적으로 막혀서 중단됨). **다음에 확인할 것**: `webapp/notify.log` 존재 여부/내용 확인, 또는 실제로 다음날 08:00에 카카오톡이 왔는지 확인. 안 되면 Task Scheduler의 cmd 인자 이스케이프 문제일 가능성이 높으므로(과거에 `cmd.exe /c "..."` 형태로 여러 번 시도해도 로그가 안 남는 이상현상이 있었음) node-cron 같은 애플리케이션 레벨 스케줄러로 전환 고려.

---

## 5.5 아이건설넷 자동 로그인 전면 중단 (2026-07-19)

**로그인 시 아이건설넷 쪽에서 경고 팝업이 뜸 — AI/자동화 접근을 감지한 것으로 사용자가 판단, 즉시 전면 중단 지시.** 더 이상 이 사이트에 로그인 시도하지 않는다 (우회 시도도 하지 않음).

조치 완료:
- Windows 작업 스케줄러 `BidAnalysis-KakaoNotify` 태스크 **비활성화**(`Disable-ScheduledTask`).
- `webapp/scripts/notify.js`: 실행 즉시 종료하도록 변경(방어적 조치, 혹시 수동 실행되어도 로그인 안 함).
- `webapp/server.js`의 `POST /api/open-bids/refresh`: 항상 410 에러 반환하도록 비활성화.
- `webapp/public/app.js`의 "실시간 새로고침" 버튼: 클릭해도 스크래핑 시도 없이 중단 안내만 표시.
- `lib/scraper.js` 코드 자체는 남겨뒀지만(참고용) 어디서도 호출되지 않음.

기존에 이미 수집된 `data/raw/*.csv`(아이건설넷 과거 낙찰 이력, 최근 3년)는 **그대로 유지·활용** — 이건 과거 데이터 분석용으로 이미 받아둔 것이라 문제 없음. 문제가 된 것은 "진행중 입찰"/"맞춤정보"를 실시간으로 긁어오던 로그인 세션 부분.

카카오톡 자동 알림(5장)은 이 스크래핑에 의존했으므로 함께 중단됨. 재개하려면 `lib/g2b.js` 기반으로 `scripts/notify.js`를 다시 연결해야 함 (현재는 미연결, 6장 참고).

---

## 5.6 나라장터 OpenAPI 연동 + 카테고리 편차 모델 + 분석 새 탭 (2026-07-19)

5.5의 대체 계획을 실제로 구현·검증 완료.

**나라장터 데이터 수집** (`webapp/lib/g2b.js`, 신규):
- 서비스: 조달청_나라장터 입찰공고정보서비스, operation `getBidPblancListInfoCnstwk` (공사 입찰공고 목록). End point/인증키는 `webapp/g2b.local.md`(gitignore, 사용자가 이미 data.go.kr에서 발급받은 키 사용).
- **중요**: 이 API를 curl로 호출하면 data.go.kr WAF가 `403 Forbidden`(일반 OpenAPI 에러 포맷이 아닌 평문)을 반환하지만, 브라우저와 Node 내장 `fetch`(undici)로는 정상 응답(200+JSON)함을 실측 확인. 클라이언트 시그니처 기반 차단으로 추정 — **반드시 fetch를 쓸 것**, curl로 재현/디버깅하려 하지 말 것.
- 최근 N일(기본 14일, `inqryDiv=1` 공고게시일시 기준) 전국 공사공고를 페이지네이션(100건씩)으로 받은 뒤, (1) `cnstrtsiteRgnNm`이 부산/경상남도로 시작 (2) `isGyeongnamCityRestricted()`(`localFilter.js` 재사용)로 경남 시/군 관내제한 발주처 제외 (3) 공고명 키워드 매칭으로 우리 업종(포장/토공/상.하수도 등) 여부 판정 (4) 마감일 지난 것 제외 — 순서로 필터링.
- 공고명 → 대업종/종목 키워드 매칭 규칙(`KEYWORD_RULES`)은 `data/raw` CSV의 실측 종목 태그 빈도(포장 2513·상.하수도 1642·토공 1329·보링.그라우팅.파일 1028·철콘 892·토건 831·토목 740건, 전체 4196행 중)를 보고 정함. 대업종 표기는 기존 `pickGroup()`이 그대로 재사용되도록 CSV와 동일한 문자열("지반조성.포장","상.하수도") 사용.
- 결과를 기존 캐시 포맷 그대로 `data/open_bids.json`(전체)/`data/mybid_list.json`(맞춤정보 재정의, 아래)에 저장 — `server.js`/`build-static.js`의 나머지 코드는 무수정으로 동작.
- 실측 검증(2026-07-19): 최근 14일 전국 공사공고 중 필터 통과 8건. 전부 포장(경상남도, 국토교통부 부산지방국토관리청 김해국토관리사무소 발주 3건) 또는 상.하수도(부산 서구/해운대구/사하구) 로 정확히 분류됨.

**맞춤정보 재정의**: 로그인 계정 저장검색 개념이 없어졌으므로, "기초금액(추정가격)이 해당 업종 2025년도 시공능력평가액(지반조성.포장 927,892천원 / 상.하수도 1,877,206천원) 이하 — 즉 실제로 수주 가능한 규모"로 재정의 (`g2b.js`의 `fitsCapacity()`). `index.html` 설명 문구도 갱신.

**카테고리 편차 반영 모델** (`webapp/lib/analysis.js`):
- 기존 `recommendBid(기초금액, 대업종)`는 포장/상하수도 2단 분류까지만 썼음(`pickGroup`). CSV의 `종목` 컬럼(대업종보다 세분화된 태그 조합, 예: "토목/토건/포장/토공/금속구조물.창호.온실/철강구조물/보링.그라우팅.파일/지붕판금.건축물조립" 같은 "/"로 이은 다중 태그 문자열)은 로딩만 되고 한 번도 안 쓰였는데, 이번에 처음 활용.
- 신규 `computeCategoryDeviation(group, matcher, baselineMean, {minSample})`: group(이미 포장/상하수도로 걸러진 풀) 중 태그를 포함하는 서브셋의 가중평균 낙찰률이 baseline과 얼마나 다른지(delta) 계산, 표본 미달(기본 15건/발주처는 8건)이면 null.
- `recommendBid(기초금액, 대업종, { 종목, 발주처 } = {})`로 시그니처 확장(하위호환, 3번째 인자 생략하면 기존과 100% 동일 동작 — 실측 확인함). 종목 태그 하나(표본 충분한 첫 번째, 과다누적 방지 위해 하나만) + 발주처(가중치 0.5배) delta를 tiers/안정회귀형/공격밀집형 전체 낙찰률에 균일 이동. 반환값에 `appliedAdjustments: [{dimension, value, delta, sampleSize}]` 추가.
- 실측 예시(김해국토관리사무소 발주 포장공사): 종목 "포장" delta -0.05%p(표본 2,499건), 발주처 "국토교통부 부산지방국토관리청 김해국토관리사무소" delta +0.11%p(표본 113건) — 사용자가 요청한 "작은 비율의 편차" 그대로 반영됨.
- `server.js`(`/api/analysis/:postingId`)·`build-static.js`(`buildItemAnalysis`) 양쪽 호출부에 `{ 종목: item.종목, 발주처: item.발주처 }` 전달하도록 갱신.

**분석 새 탭** (진행중 입찰 항목만, 맞춤정보는 기존처럼 인라인 유지 — 사용자 결정):
- `webapp/public/analysis-render.js`(신규, 공용 렌더 모듈): `app.js`에 있던 `seriesColor`/`cssVar`/`fmtWon`/`fmtRatioAsPct`/`renderStrategyTiles`/`renderCompanyPredictions`를 옮기고, `renderAppliedAdjustments`(위 편차 내역 표시)와 `renderAnalysisBody`(전체 조립)를 추가. `index.html`/`analysis.html` 모두 이 스크립트를 `app.js`/`analysis.js`보다 먼저 로드.
- `webapp/public/analysis.html` + `analysis.js`(신규): `?id=` 쿼리로 `/api/analysis/{id}.json`을 그대로 fetch(서버 라우트 무수정), `loadMe()` 인증 체크. 진행중 입찰 카드 클릭 시 `window.open('/analysis.html?id=...', '_blank')`.
- Playwright로 로그인→"진행중 입찰" 카드 클릭→새 탭에서 분석 페이지(추천가/전략밴드/편차 내역/TOP5 예측 전부 정상 표시) 실측 확인함.

(2026-07-19 후속 세션에서 자동 갱신·카카오 알림까지 구현 — 5.7장 참고.)

---

## 5.7 완전 클라우드 자동 갱신 + 신규 입찰 카카오 알림 (Cloudflare Cron, 2026-07-19)

**배포된 Worker가 매일 08시(KST)에 스스로 나라장터를 조회·갱신하고, 신규 입찰이 생기면 분석 요약 + 리포트 링크를 사용자 카카오톡으로 자동 발송한다.** PC를 켜둘 필요 없음(완전 클라우드).

**아키텍처**:
- `wrangler.toml`: `[triggers] crons = ["0 23 * * *"]`(23:00 UTC = 08:00 KST), `compatibility_flags=["nodejs_compat"]`(공용 lib의 `require('fs')` 폴리필용 — Worker 실행경로에선 fs 호출 안 함), `[[rules]] type="Text" globs=["**/*.csv"]`(과거 이력 CSV를 번들에 내장).
- `webapp/worker/index.js`: 상단에서 CSV를 Text 임포트 → `analysis.loadHistoryFromText()` 1회. `scheduled()` 핸들러가 cron 진입점. 데이터 API(`/api/open-bids.json`,`/api/mybid-list.json`,`/api/analysis/:id`)는 **KV 우선, 없으면 정적 폴백**. `POST /api/open-bids/refresh`(수동 새로고침)도 Worker가 처리(카카오 발송은 안 함).
- `refreshBids(env,{notify})`: 나라장터 조회→`buildBidLists`→이전 KV(`open_bids`)와 posting_id 비교로 신규 판정→KV에 `open_bids`/`mybid_list` + 공고별 `analysis:{id}`(사전계산, 요청당 CPU 최소화) 저장. notify=true(cron)면 신규건 카카오 발송. **최초 실행(이전 KV 없음)은 baseline만 저장, 알림 생략.**
- 카카오: `analysis:{id}`는 미리 계산해두므로 알림 메시지는 분석 요약(추정예정가격/추천가80%/안정회귀중앙값/편차) + 리포트 링크(`analysis.html?id=`). 신규 ≤5건은 개별 발송, >5건은 합본 1건. access_token은 `createKakaoClient` 인스턴스에 메모이즈 → run당 refresh 1회. **분석·요약 전부 순수 통계·문자열 = AI 토큰 0.**

**리팩터링(로컬/Worker 공용, fs 주입식)**: `lib/analysis.js`(`parseHistory`/`loadHistoryFromText` 분리), `lib/g2b.js`(`fetchRecentCnstwkBids({serviceKey})`+`buildBidLists` 순수함수), `lib/kakao.js`(`createKakaoClient({restApiKey,loadTokens,saveTokens})` 팩토리), `lib/notifyMessage.js`(g2b 필드 반영 + `buildBidAnalysisMessage`/`buildNewBidsSummaryMessage`). top-level `path.join(__dirname,...)`은 번들 안전성 위해 지연 함수로 이동. server.js는 하위호환 export 덕에 무수정.

**시크릿/시드(Cloudflare)**: `wrangler secret put G2B_SERVICE_KEY`, `KAKAO_REST_KEY`. KV `kakao_token`에 로컬 `data/kakao_token.json` 시드(`--path`). 로컬 `wrangler dev`용은 `.dev.vars`(gitignore).

**검증 완료**: `wrangler dev --test-scheduled`로 cron 트리거 → `[cron] 나라장터 8건, 신규 8건 (최초 실행 — 알림 생략)` 확인. 로컬 admin 로그인 후 `/api/open-bids.json`(KV, 8건)·`/api/analysis/{id}.json`(사전계산, 편차+TOP5)·`/api/mybid-list.json` 정상. 배포(버전 `ac4bf7ed`, gzip 738KB, startup 83ms, `schedule: 0 23 * * *` 등록) 후 login 307·`/api/*` 401(인증 정상) 확인.

**프로덕션 실물 검증 완료(2026-07-19)**: baseline을 "현재 8건 - 1건"으로 KV 시드 후 cron을 임박 시각으로 임시 변경·발화 → `[cron] 나라장터 8건, 신규 1건` + invocation Ok, 신규 1건의 분석 메시지가 실제 카카오로 발송됨(사용자 수신 확인). cron은 `0 23 * * *`로 원복. (1차 시도는 `Too many subrequests`로 실패 → `numOfRows` 100→999로 페이지 수 감소시켜 해결, 커밋 `7555690`.)

**카카오 링크 도메인 등록 — 해결 완료(2026-07-19)**: 카카오 메시지의 링크(`web_url`)는 카카오 앱의 [플랫폼 > Web > 사이트 도메인]에 등록된 도메인이어야 정상 동작. 초기엔 `http://localhost:4173`만 등록돼 있어(OAuth 잔재) "분석 리포트 열기"가 localhost로 열렸음. **사용자가 카카오 콘솔에서 `https://bid-analysis.bisan74.workers.dev`를 사이트 도메인에 추가** → 테스트 재발송으로 배포 리포트 페이지가 정상 열림을 확인함. (코드는 처음부터 배포 URL을 보냈고, `worker/index.js`의 `DASHBOARD_URL`은 `env.DASHBOARD_URL`로 재정의 가능. 카카오 앱 site domain 등록 방법은 `webapp/kakao.local.md`에도 기록.)

---

## 5.8 기초금액 기준 정립 + 복수예비가격 예정가격 분포 + A값 투찰금액 (2026-07-23)

2026-07-20 통화 녹취와 다른 AI의 예비가격 산정방법 분석을 반영. 상세 원문 정리는 `BIDDING_PRICE_METHOD.md`.

**핵심 배경(정석 로직)**: 발주기관이 기초금액(부가세 포함, 공개)을 산정 → 기초금액 ±2%(국가)/±3%(지방) 범위에서 난수 사정률로 **15개 복수예비가격** 생성 → 입찰자들이 각자 2개 선택 → **가장 많이 선택된 4개의 산술평균 = 예정가격** → **투찰금액 = (예정가격 − A값) × 낙찰하한율 + A값** (A값=노무비·보험료·산업안전보건관리비 등 낙찰률 할인 제외 고정 실비).

**반영 3건**:
1. **기준 금액을 기초금액으로 고정** (녹취 핵심 지적: "추정가격으로 하면 괴리가 너무 커짐"). 나라장터 목록조회는 추정가격(부가세 제외)만 주므로, 과거이력 9,636건 실측(94.7%가 정확히 1.1, 중앙값·P5·P95=1.1)을 근거로 **기초금액=추정가격×1.1**로 복원. `lib/g2b.js` `normalizeItem()`에서 `기초금액`/`기초금액추정` 세팅(상수 `ESTIMATE_TO_BASE`). 이 한 곳 수정으로 server/worker/build-static/notify 소비처 전부 교정됨. (기존 cache/KV는 다음 새로고침 때 갱신.)
2. **예정가격 확률 분포**: `lib/analysis.js` `computeJeonggaDistribution()` → `recommendBid()` 반환에 `예정가격분포`(P10~P90 예가율/사정률/예정가격) 추가. 15개 예비가격 다빈도 4개 평균의 난수성을 과거 예가율 분포로 밴드 표시. `analysis-render.js` `renderJeonggaDistribution()`.
3. **A값 투찰금액 계산기**: `analysis.js` 순수함수 `computeTuchalAmount(예정가격,낙찰하한율,A값)` (AI 대화 검증값 84,149,695원 일치). 상세분석에 입력식 계산기 위젯(`renderAValueCalculator`, `krsCalcTuchal`) 추가. 과거 낙찰률 tiers/전략밴드는 A값 효과가 이미 경험적으로 녹아 있어 A값 없이도 동작; 계산기는 특정 공고 정확 투찰금액 산출용.

**미해결(향후)**: 15개 예비가격 구간별 "선택확률" 모형(녹취 개선안 최종 단계)은 회차별 예비가격·선택집계 원자료가 없어 미구현 — 현재는 결과(예정가격)의 확률 밴드까지만. A값은 공고문에서 수동 입력 필요.

**AI최종 전수 검증 대시보드 반영 (2026-07-25)**: 150건 표본과 별개로 과거 이력 **4,157건 전수 leave-one-out 검증**을 추가. `analysis.validateFullHistory()`(순수계산, ~0.7초, fs 비의존) → `scripts/validate-full-history.js`가 `data/ai-prediction/full_history_report.json` 생성, `server.js`/`build-static.js`가 `/api/ai-prediction/full-report.json`로 서빙(파일 없으면 즉석 계산). 대시보드 AI 검증 섹션에 "AI최종 전체 이력 전수 검증" 카드(6타일: 표본수/MAE/±0.5·1·2·3% 적중) 추가(`renderAiFullHistory`). **결과: MAE 0.5775%, ±0.5% 50.7%, ±1% 83.3%, ±2% 99.7%, ±3% 100%** — 150건 검증(0.599%)과 일치해 표본 편향 없음을 교차확인. AI 추론(Claude 개별 추론)을 4,157건에 돌리는 건 토큰·시간 막대 + 실익 없음(150건서 잡음 확인)이라 미실시, 통계 전수검증으로 대체.

**AI최종 모델 확정 (2026-07-25)**: 지금까지 제안된 모든 신호(발주처 shrinkage k=10~200·유의성 게이트, 지역/금액구간/종목태그 편차, 가중평균 vs 가중중앙값)를 **과거 이력 4,157건 leave-one-out으로 실측 채점**해 이기는 신호만 수렴한 최종 모델 `predictJeonggaFinal()` 추가. **채택**: 기초금액 기준(×1.1 복원) + 풀 가중**중앙값**(MAE 최적 점추정 원리; LOO 전체 0.5772% vs 평균 0.5774%, 최근1년 0.5820% vs 0.5824%, 150건 검증 0.5986% vs 0.5989% — 전부 일관 우위). **배제(실측 근거)**: 발주처 편차(전 k에서 악화 0.5788~0.5799%, 게이트로도 못 이김 — 예가율은 낙찰률과 달리 발주처 신호가 재현되지 않음), 지역/금액구간/종목태그(±0.0007%p 잡음), 참여업체수(입찰 전 알 수 없는 사후 데이터 누수). 남는 ~0.58%는 복수예비가격 난수의 본질적 폭 = 점추정 이론 한계 → 예정가격분포(P10~P90)와 병행이 정석. UI: 대시보드 검증 섹션에 `AI최종` 아이콘(보라-파랑 `.badge-ai-final`) 타일/차트 4번째 시리즈/표 열 추가, 공고 상세 리포트(`renderAnalysisBody`)에 "AI최종 예정가격 예측" 칸 추가(`recommendBid` 반환에 `aiFinal`). score_report.json에 `aiFinal` 요약 + rows에 AI최종 3필드. 실험 스크립트는 스크래치패드에서 실행(레포 미포함), 수치는 본 문단이 기록.

**AI 예정가격 예측 재검증 + 개선 통계모델 (2026-07-23)**: 기존 150건 AI 추론 예측(2026-07-05, MAE 0.596%)을 유지한 채, AI가 수동으로 하던 "발주처별 예가율 편차 → 축소추정(shrinkage, k=20, leave-one-out)"을 코드로 재현한 `analysis.predictJeonggaPrice()`를 신규 추가하고 채점(`scripts/score-ai-predictions.js`)에 3열 비교(AI추론/기존baseline/개선모델)로 편입. 결과: **AI 0.5958% / 기존 0.5987% / 개선 0.5991%** — 세 모델 사실상 동률. 예정가격은 복수예비가격 추첨이라 **설계상 준-랜덤**이라 발주처/종목 패턴으로도 유의미하게 더 못 맞힌다는 이론을 실측으로 확증(개선모델이 baseline을 근소하게 못 이김). 대시보드 "AI 예정가격 예측 검증" 섹션에 개선모델 타일·차트 막대·표 열 추가, 신규 반영분엔 `NEW` 배지 표기(`.badge-new`). score_report.json에 `improved` 요약 + rows에 `개선모델예측예정가격/개선모델오차율/개선모델근거` 추가.

---

## 6. 알려진 제약사항 / 미결정 사항

- **아이건설넷 로그인 전면 중단**(5.5장) → 나라장터 연동(5.6장) + 클라우드 자동 갱신·카카오 알림(5.7장)으로 대체 완료.
- 배포본은 이제 **KV 기반 실시간**(정적 스냅샷 모드 폐지). KV가 비면 build-static이 만든 정적 파일로 폴백. `webapp/data/snapshot/*`은 이 폴백용으로만 유지.
- 실제 카카오 발송은 아직 프로덕션에서 1회도 안 나감(5.7장 미검증 참고).
- 카카오 refresh_token은 cron이 매일 돌면 자동 롤오버되어 만료 안 됨. cron이 ~30일 이상 중단되면 재인증 필요(엣지 케이스).
- 안정회귀형/공격밀집형 밴드폭(SEM 배수, 히스토그램 bin폭 0.25%p)은 임의로 정한 파라미터 — 통계적으로 정교화 여지 있음.
- TOP5 업체 예측(`predictCompanyBid`)은 업체의 **낙찰 건**만 학습 데이터로 씀(그 업체가 참여했지만 떨어진 건은 원천적으로 알 수 없음 — 아이건설넷 데이터 자체가 낙찰자만 보여줌). 따라서 "그 업체가 얼마면 이겼을지"의 근사치이지, 그 업체의 실제 내부 전략을 아는 건 아님.
- 공동인증서 로그인은 자동화 불가(ID/PW 로그인만 가능) — 아이건설넷 자체 제약.
- 안정회귀형/공격밀집형 밴드폭(SEM 배수, 히스토그램 bin폭 0.25%p)은 임의로 정한 파라미터 — 통계적으로 정교화 여지 있음.
- TOP5 업체 예측(`predictCompanyBid`)은 업체의 **낙찰 건**만 학습 데이터로 씀(그 업체가 참여했지만 떨어진 건은 원천적으로 알 수 없음 — 아이건설넷 데이터 자체가 낙찰자만 보여줌). 따라서 "그 업체가 얼마면 이겼을지"의 근사치이지, 그 업체의 실제 내부 전략을 아는 건 아님.
- 공동인증서 로그인은 자동화 불가(ID/PW 로그인만 가능) — 아이건설넷 자체 제약.

---

## 7. 실행 방법 (요약)

```bash
cd webapp
npm install                      # 최초 1회 (완료됨)
npx playwright install chromium  # 최초 1회 (완료됨)
npm start                        # http://localhost:4173
```

서버가 이미 떠 있는지 확인: `curl -s -o /dev/null -w "%{http_code}" http://localhost:4173/` (200이면 정상).
새로고침(진행중입찰+맞춤정보 재스크래핑): 대시보드의 "실시간 새로고침" 버튼, 또는 `curl -X POST http://localhost:4173/api/open-bids/refresh`.

---

## 8. 참고: 세부 변경 이력이 필요하면

이 문서는 "현재 상태" 스냅샷이고, **왜 이렇게 됐는지의 시행착오 과정**(카카오 콘솔 UI 삽질, 각 버그를 어떻게 찾았는지 등)은 `PROGRESS.md`에 세션별로 기록되어 있음. 짧은 파일별 포인터는 `CLAUDE.md` 참고.
