# PROGRESS

세션마다 무엇을 했는지, 왜 그런 선택을 했는지 짧게 기록합니다. (git log는 "무엇"을, 이 파일은 "왜"와 "다음에 뭘 할지"를 담당)

## 2026-07-04

- 프로젝트 시작. 토목건설 수주 입찰가 분석 프로젝트 폴더 생성.
- 사용자가 제공한 4개 확인서(경영상태등의 확인서, 신용평가등급 확인서, 시공능력평가확인서 x2)를 `data/company-profile.md`, `data/license-capacity.csv`로 정리.
- 아이건설넷(igunsul.net) 로그인 정보 전달받음 → `credentials.local.md`에 보관 (gitignore 처리). 단, WebFetch 도구는 인증 세션을 유지할 수 없어 로그인이 필요한 페이지를 직접 크롤링할 수 없음을 확인.
- 데이터 수집 방식 논의: 아이건설넷 로그인 페이지 확인 결과, ID/PW 로그인과 공동인증서(보안프로그램) 로그인 2가지 지원. 공동인증서는 자동화 불가, ID/PW는 브라우저 자동화 도구가 있으면 시도 가능.
- 사용자가 브라우저 자동화 시도를 선택 → `claude mcp add playwright -- npx -y @playwright/mcp@latest` (local scope, `projects/` 상위 폴더 기준)로 Playwright MCP 서버 등록. **세션 재시작 후에야 도구가 로드됨** — 재시작 후 로그인 자동화 시도 예정. 실패 시(보안프로그램/캡차 등으로 막히면) `data/raw/` 수동 업로드 방식으로 폴백.
- (다음 세션) local scope 등록이 `C:\Users\bisan\projects`에 걸려 있어서 이 세션(cwd: 홈 디렉터리)에서는 도구가 안 잡힘 → `--scope user`로 재등록해 어느 디렉터리에서 세션을 열어도 항상 로드되게 함(`claude mcp list` 확인 결과 연결 정상). 단, **현재 대화 세션은 도구 목록이 세션 시작 시점에 고정**되므로, 실제로 브라우저 자동화 도구를 쓰려면 새 세션을 시작해야 함.
- 면허별(지반조성포장/상하수도설비) 시공능력평가액 기준 1차 분류표 작성.
