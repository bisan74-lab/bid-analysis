# PROGRESS

세션마다 무엇을 했는지, 왜 그런 선택을 했는지 짧게 기록합니다. (git log는 "무엇"을, 이 파일은 "왜"와 "다음에 뭘 할지"를 담당)

## 2026-07-04

- 프로젝트 시작. 토목건설 수주 입찰가 분석 프로젝트 폴더 생성.
- 사용자가 제공한 4개 확인서(경영상태등의 확인서, 신용평가등급 확인서, 시공능력평가확인서 x2)를 `data/company-profile.md`, `data/license-capacity.csv`로 정리.
- 아이건설넷(igunsul.net) 로그인 정보 전달받음 → `credentials.local.md`에 보관 (gitignore 처리). 단, WebFetch 도구는 인증 세션을 유지할 수 없어 로그인이 필요한 페이지를 직접 크롤링할 수 없음을 확인.
- 데이터 수집 방식 논의: 아이건설넷 로그인 페이지 확인 결과, ID/PW 로그인과 공동인증서(보안프로그램) 로그인 2가지 지원. 공동인증서는 자동화 불가, ID/PW는 브라우저 자동화 도구가 있으면 시도 가능.
- 사용자가 브라우저 자동화 시도를 선택 → `claude mcp add playwright -- npx -y @playwright/mcp@latest` (local scope, `projects/` 상위 폴더 기준)로 Playwright MCP 서버 등록. **세션 재시작 후에야 도구가 로드됨** — 재시작 후 로그인 자동화 시도 예정. 실패 시(보안프로그램/캡차 등으로 막히면) `data/raw/` 수동 업로드 방식으로 폴백.
- (다음 세션) local scope 등록이 `C:\Users\bisan\projects`에 걸려 있어서 이 세션(cwd: 홈 디렉터리)에서는 도구가 안 잡힘 → `--scope user`로 재등록해 어느 디렉터리에서 세션을 열어도 항상 로드되게 함(`claude mcp list` 확인 결과 연결 정상). 단, **현재 대화 세션은 도구 목록이 세션 시작 시점에 고정**되므로, 실제로 브라우저 자동화 도구를 쓰려면 새 세션을 시작해야 함.
- 면허별(지반조성포장/상하수도설비) 시공능력평가액 기준 1차 분류표 작성.
- 정리된 자료(회사 프로필, 시공능력평가액, 신용등급 추이)를 로컬에서 볼 수 있는 단일 HTML 리포트(`report.html`) 작성. 외부 리소스 없이 오프라인 열람 가능, 다크모드 대응, 차트 호버 툴팁·표 보기 토글 포함. 회사 정보 자체는 사용자 요청으로 추가 수정 보류 (나중에 사용자가 직접 수정 예정).
- **같은 대화 세션 안에서는 새로 등록한 MCP 도구가 로드되지 않음을 재확인.** 아이건설넷 데이터는 아직 다운로드/데이터화 전 (`data/raw/` 비어있음) — Playwright 자동화를 실제로 쓰려면 사용자가 Claude Code 대화 패널을 닫고 새 세션을 열어야 함. **다음 세션 첫 할 일: 아이건설넷 로그인 자동화 시도 → 성공 시 과거 낙찰 데이터 수집, 실패 시 수동 업로드(`data/raw/`)로 폴백.**

## 2026-07-04 (세션 2)

- 새 세션에서 Playwright MCP 도구 정상 로드 확인. `credentials.local.md`의 ID/PW로 아이건설넷 홈페이지에서 로그인 자동화 성공 (아이디/비밀번호 입력 → 로그인 버튼 클릭, "(주)케이알에스건설 님" 표시로 확인).
- 낙찰정보(`/nbid`) → 조건검색에서 종목=지반조성.포장 OR 상.하수도, 12개월로 전국 검색 시도 → **최근 1주일치만으로 500건(1페이지 한도) 도달**, 전국+12개월은 수만 건 규모로 브라우저 자동화로 전량 수집 비현실적임을 확인.
- 사용자에게 범위 확인 후 **지역을 부산+경남으로 한정**하기로 결정. 아이건설넷 조건검색 폼은 지역 단일선택만 가능해 부산/경남 각각 따로 검색 후 병합.
  - 부산: 12개월, 2페이지, 872건 (2025-07-04 ~ 2026-07-03 전체 커버)
  - 경남: 12개월, 5페이지, 2,186건 (2025-07-04 ~ 2026-07-03 전체 커버)
- 결과 테이블은 `document.querySelectorAll('table')` + Playwright `browser_evaluate`로 DOM에서 직접 탭 구분 텍스트로 추출 (접근성 스냅샷은 500행 기준 2만 줄 이상이라 비효율적). bbscode+공사명+개찰일 기준 중복 제거, 두 지역 합쳐 총 3,058건 중복 없음 확인.
- 병합 CSV를 `data/raw/nbid_busan_gyeongnam_12mo_20260704.csv`에 저장 (컬럼: bbscode, 공사명, 종목, 대업종, 발주처, 지역약칭, 기초금액, 추정가격, 예정가격, 사정률, 1순위금액, 1순위사정률, 1순위업체, 참여업체수, 개찰일, 검색지역).
- **다음 세션 할 일**: 이 원자료를 정제(금액 숫자 파싱, 사정률 분포 분석)해서 우리 회사 면허(지반조성포장/토공, 상하수도설비) 기준 낙찰 확률이 높은 사정률/투찰률 구간을 추정하고 `report.html`에 반영.

## 2026-07-04 (세션 3) — 데이터 대시보드 웹앱 구축

- **1~3년전(2년치) 낙찰 데이터 추가 수집**: 부산 2,018건 + 경남 4,686건. 기존 12개월치(부산 872 + 경남 2,186)와 합쳐 **총 9,666건**(중복 제거) 확보. 커스텀 날짜 범위는 `#ipchal_date1`/`#ipchal_date2` input에 직접 값 세팅 후 change 이벤트 디스패치하는 방식으로 처리(달력 위젯 클릭 불필요).
- **중요 발견**: 검색 결과 테이블의 "번호" 컬럼은 실제 공고 ID가 아니라 **검색 결과 내 순번**(매 검색마다 새로 매겨짐). 여러 검색을 병합할 때는 공사명 안의 대괄호 코드(예: `[R26BK01608011-000]`)를 실제 고유 ID로 추출해서 중복 제거해야 함.
- 최종 데이터: `data/raw/nbid_busan_gyeongnam_3y_20260704.csv` (컬럼에 `연차구분`: 최근1년/1~2년전/2~3년전 태그 포함). 기존 12개월 단독 CSV는 삭제(3년 파일이 상위 호환).
- **`webapp/` 폴더에 Node.js/Express 기반 로컬 대시보드 웹앱 구축**:
  - `lib/analysis.js`: CSV 로딩 + 통계. 가중치는 **반감기 1년 지수가중**(최근1년 57.1% / 1~2년전 28.6% / 2~3년전 14.3%)으로 정의. 낙찰률=1순위금액÷예정가격, 예가율=예정가격÷기초금액을 직접 계산(원본 "사정률"/"1순위사정률" 컬럼은 정의가 불명확해 사용 안 함). 진행중 공고의 기초금액에 가중평균 예가율을 곱해 예정가격을 추정하고, 가중 백분위수(100/80/65/50%)를 곱해 확률별 추천 입찰가 산출.
  - `lib/scraper.js`: `playwright` npm 패키지(MCP와 별개로 설치, headless Chromium 다운로드 완료)로 로그인 자동화 + `/bid/conditional_search`의 실제 `<select name="part/part2/local">` 필드를 직접 조작해 폼 제출(종목: 지반조성.포장=B001, 상.하수도=B008 / 지역: 부산=14, 경남=12 코드 확인됨). 페이지네이션은 `a.list2pageAnchor[page_num]` 클릭 방식. **주의**: 검색 결과 테이블이 JS로 점진적으로 채워지므로 row 수가 안정될 때까지 폴링 대기 필요(단순 waitForSelector는 너무 일찍 반환됨).
  - `server.js`: Express API — `/api/history/stats`(대시보드 차트용 집계), `/api/open-bids`(캐시된 진행중 목록), `/api/open-bids/refresh`(POST, 실제 재로그인+스크래핑), `/api/open-bids/:id/analysis`(개별 공고 추천 입찰가).
  - `public/`: 순수 vanilla JS + 손수 작성한 SVG 차트(외부 차트 라이브러리 없음). dataviz 스킬의 팔레트/폼 가이드 적용, 라이트/다크 모드 대응.
- **실행 방법**: `cd webapp && npm install && npx playwright install chromium && npm start` (최초 1회) → `node server.js` 로 http://localhost:4173 접속. 최초 npm install/브라우저 설치는 이미 완료된 상태.
- 대시보드 구성: 핵심지표 카드, **진행중 입찰 목록(실시간, "새로고침" 버튼으로 재스크래핑)** + 클릭 시 낙찰확률 100/80/65/50% 추천 입찰가 표시, 방법론 설명, 연차별/지역별/업종별/월별/낙찰률분포/금액구간/발주처TOP15/시군구TOP20/참여업체수-낙찰률 산점도 차트, 원자료 표(접근성용).
- 검증: Playwright MCP로 브라우저 접속해 스크린샷 확인 — 라이트/다크 모드 정상, 콘솔 에러 없음(초기 SVG height="auto" 속성 오류는 style로 이동해 수정), 카드 클릭 → 분석 패널 정상 표시 확인.
- **다음에 할 일**: 사용자가 실제 사용해보고 불필요한 섹션/차트는 정리 예정("나중에 불필요하다고 판단되는 부분은 삭제"). 서버를 상시 구동해두려면 Windows 작업 스케줄러 등록이나 PM2 같은 프로세스 매니저 고려 가능.

## 2026-07-05 — 관내 필터링 + 전략 구간 추가

- **경남 시/군 단위 발주처 제외**: 부산 소재 회사는 경남 관내(시/군 등록업체 한정) 공고에 입찰 자격이 없다는 사용자 지적 반영. `webapp/lib/localFilter.js` 신설 — "경남/경상남도 + 시/군명" 또는 "시/군명으로 시작하는 발주처명"만 제외(관내 제한으로 추정), "경상남도"/"경남지역본부 OO지사"처럼 시/군명이 뒤에 안 붙거나 전국단위 공기업 지역사무소인 경우는 유지. 단순 부분 문자열 매칭은 오탐(예: "산청"이 "해양수산청"에 포함되는 문제, "한국농어촌공사 경남지역본부 OO지사"를 시/군 발주처로 오인)이 발생해 정규식으로 경계를 명확히 함.
  - 적용 후: 이력 데이터 9,666건 → **4,208건**(부산 2,882 + 경남 1,326)으로 축소. `lib/analysis.js`(loadHistory)와 `lib/scraper.js`(진행중 목록) 양쪽에 동일 필터 적용.
- **입찰가 추천에 전략 구간 2종 추가** (사용자가 Gemini와 나눈 사정률 분석 대화를 참고해 통계적으로 재정의):
  - **안정회귀형**: 가중평균 낙찰률(=평균 회귀 기대 중심값) ± 가중 표준오차(SEM, 유효표본수 보정)로 하한선/중앙값/상한선 3개 산출. "평균으로 돌아올 것"이라는 기대에 기반한 보수적 밴드.
  - **공격밀집형**: 낙�찰률 히스토그램(0.25%p 단위, 가중치 적용)에서 가장 밀도가 높은 구간을 찾아 그 구간 경계를 그대로 하한선/상한선으로 사용. 평균과 무관하게 "실제로 가장 많이 낙찰된 지점"을 노리는 전략.
  - 기존 100/80/65/50% 확률 4칸 아래에 이 5개 칸(하한/중앙/상한 3 + 하한/상한 2)을 추가 표시. `lib/analysis.js`의 `recommendBid()`가 `stableRegression`/`aggressiveCluster` 필드로 반환.
- 서버 재시작 + 진행중 목록 재스크래핑(새 필터 적용) 후 확인: 부산/경남 진행중 24건 → 17건으로 정상 축소, 9칸 분석 패널 정상 렌더링 확인.

## 2026-07-05 — 공유/배포 방법 논의 + 카카오톡 "나에게 보내기" 자동 알림 구축

- **공유/배포 방법 논의**: 사용자가 "다른 사람과 공유", "모바일 조회", "카카오톡 연계" 3가지를 문의. 서버 배포(Cloudflare Tunnel / 클라우드 VM / PaaS 비교)는 **아직 결정 보류** 상태 — 다음에 다시 논의 필요. 카카오톡 연계는 **"본인에게 자동 알림(무료, 나에게 보내기 API)"** 방식으로 진행하기로 결정.
- **카카오톡 "나에게 보내기" 연동 완료**:
  - `webapp/lib/kakao.js`: OAuth 인가코드 교환/토큰갱신/`sendToMe()` 구현. 설정은 `webapp/kakao.local.md`(REST API 키, gitignore 처리)에서 읽음. 토큰은 `webapp/data/kakao_token.json`(gitignore 처리)에 저장.
  - `webapp/lib/notifyMessage.js`: 진행중 입찰 요약 메시지 텍스트 포맷터 (server.js 테스트 발송과 scripts/notify.js 공용).
  - `webapp/scripts/notify.js`: 진행중 입찰 새로고침 → 카카오톡 발송을 한 번에 수행하는 단독 실행 스크립트.
  - `server.js`에 `/oauth/kakao/start`(인가 URL로 리다이렉트), `/oauth/kakao/callback`(토큰 교환), `POST /api/notify/test`(테스트 발송) 라우트 추가.
  - **카카오 디벨로퍼스 설정 시행착오 기록** (다음에 새 앱을 또 만들 일이 있으면 참고): Redirect URI 등록 위치가 "카카오 로그인 > 일반"이 아니라 **"앱 > 플랫폼 키" 페이지의 REST API 키 카드를 클릭해서 들어간 상세 화면**에 있었음(콘솔 UI가 개편되어 예전 문서/기억과 다른 위치에 있었음). 또한 Client Secret이 켜져 있으면 토큰 교환 시 `KOE010 invalid_client` 에러가 남 — 개인용 자동화라 Client Secret은 "사용 안 함"으로 끔.
  - REST API 키는 사용자가 카카오 디벨로퍼스에서 직접 발급받아 전달, `kakao.local.md`에 저장됨(git 미포함). OAuth 1회 인가(브라우저로 본인 카카오 로그인 후 동의)도 완료, `POST /api/notify/test`로 실제 카카오톡 수신 확인함.
- **Windows 작업 스케줄러 등록**: 매일 08:00 `scripts/notify.js` 실행되도록 태스크명 `BidAnalysis-KakaoNotify` 등록.
  - **주의(미해결 이슈)**: `cmd.exe /c "node ... >> notify.log 2>&1"` 형태의 Action Argument로 등록했을 때 `Get-ScheduledTaskInfo`의 `LastTaskResult`는 0(성공)으로 나오는데 정작 `notify.log` 파일이 생성되지 않는 문제가 반복됨 (수동으로 동일 명령을 `Start-Process`/PowerShell에서 직접 실행하면 정상 동작). 절대경로로 바꿔도 동일 증상이라 인자 따옴표 문제로 추정하고, **최종적으로 `webapp/scripts/run_notify.bat` 배치파일(내부에서 cd 후 node 실행+리다이렉트)을 만들어 Task의 Execute를 그 배치파일로 직접 지정**하는 방식으로 변경 — 이 방식이 실제로 카카오톡 발송까지 성공하는지는 **다음 세션에서 로그 파일 생성 여부/실제 수신 여부로 재확인 필요** (마지막 테스트 도중 PowerShell 도구의 안전성 검사기가 일시적으로 불가해져서 검증을 끝까지 못함).
  - **다음 세션 첫 할 일**: ① `Test-Path C:\Users\bisan\projects\bid-analysis\webapp\notify.log` 및 내용 확인, 또는 작업 스케줄러 GUI(`taskschd.msc`)에서 "BidAnalysis-KakaoNotify" 태스크의 "기록" 탭으로 실제 에러 확인. ② 안 되면 배치파일 대신 node-cron 같은 애플리케이션 레벨 스케줄러로 전환 고려 (Task Scheduler 인자 이스케이프 문제를 피할 수 있음). ③ 정상 확인되면 매일 08:00 자동 수신되는지 실제로 하루 기다려서 검증.
- 서버(`node server.js`, PID 확인은 `netstat -ano | grep 4173`)는 현재도 로컬에서 계속 구동 중 — 컴퓨터를 끄면 대시보드도 죽고 예약된 알림도 안 나감(로컬 실행 구조의 한계, 위 "배포 방법 논의"와 연결됨).

## 2026-07-05 (이어서) — 경남 필터 정교화 + 참여업체수 분석 강화 + 배터리로 인한 조기 종료

- **경남 시/군 필터 버그 2건 발견 및 수정** (`lib/localFilter.js`, 사용자가 대시보드의 "시·군·구 TOP20" 차트에서 진주·김해가 여전히 보이는 것을 지적하며 발견):
  1. 기존 "경남/경상남도 바로 뒤에 시/군명+시/군 접미사" 방식은 "경상남도교육청 경상남도진주교육지원청", 개별 학교("삼현여자고등학교" 등)처럼 다른 표기 형태를 놓쳤음.
  2. **더 심각한 버그**: 화이트리스트 재설계 중 "해군"(Navy) 패턴이 "남해군"(Namhae County)의 끝 두 글자와 우연히 매칭되어, **남해군청 발주 건 97건이 전부 잘못 유지**되고 있었음 (마치 "산청"이 "수산청"에 매칭되던 것과 같은 종류의 문자열 겹침 버그).
  - **최종 로직** (default-exclude, whitelist-rescue 방식): 발주처명에 경남 시/군명이 하나라도 들어있으면 **기본적으로 제외**하되, 국가/도(광역) 단위로 확인되는 기관(한국OO공사류 공기업, "도로관리사업소"/"항만관리사업소", "교육청"/"교육지원청"(도교육청 산하로 간주), "소방서"/"소방본부", 군부대(문자열 시작 위치로 한정: `^(국방부|국군|육군|해군|공군)`), 중앙정부 부처/지방청, 국립대학교, "주식회사"(민간기업))만 화이트리스트로 유지. 군부대 패턴은 반드시 **문자열 시작 위치**로 앵커링해야 "남해군" 같은 우연한 겹침을 피할 수 있음 — 비슷한 종류의 지명 문자열 매칭 버그가 또 있을 수 있으니 향후 유사 이슈 생기면 이 패턴을 참고할 것.
  - 검증: 부산 태그 건에 대한 오탐 0건 확인, "남해군" 등 시/군 자체가 유지 목록에 남아있지 않은지 재확인 완료. 필터 적용 후 전체 4,196건(부산 2,882 + 경남 1,314).
- **"참여업체수 vs 낙찰률" 분석 강화**: 기존 산점도 카드 옆에 "참여업체수 구간별 평균 낙찰률" 바 차트를 추가(`lib/analysis.js`의 `participantBins`, `scatterCorrelation`). 흥미로운 결과: 상관계수 약 +0.088(거의 무관~약한 양의 상관)로, **경쟁이 치열해질수록(참여업체 많을수록) 낙찰률이 오히려 소폭 높아지는 경향**(1~20개 참여 시 평균 -10.75% 사정 → 400개 초과 시 평균 -10.44%) — 직관과 반대되는 결과라 대시보드에 이 인사이트를 텍스트로도 표시해둠.
  - 이 작업 중 `public/app.js`의 `barChart()`가 **음수 전용 데이터(모든 값이 음수)에서 막대 높이가 음수가 되어 SVG 렌더링 에러**를 내는 버그 발견 → 0 기준선이 차트 어디에나 올 수 있도록(양수/음수 혼합 지원) 로직 수정 완료, 재검증해서 콘솔 에러 0건 확인함.
- ~~다음 세션 최우선 작업: TOP3 업체 분석~~ → **바로 아래 "TOP5 우수 업체 분석 + 예측 입찰가 기능 완성" 섹션에서 완료됨** (같은 날 배터리 충전 후 이어서 진행, TOP3가 아니라 TOP5로 확장됨).
- 카카오톡 자동 알림(Windows 작업 스케줄러) 관련 미해결 이슈는 **여전히 미해결 상태** — 위 2026-07-05 첫 기록 참고, `webapp/notify.log` 확인부터 시작. (이 항목은 아직 다음 세션 최우선 작업으로 유효함)
- 세션이 사용자 노트북 배터리 부족으로 여기서 종료됨. 서버(`node server.js`)는 로컬에서 계속 켜둔 채로 종료.

## 2026-07-05 (이어서, 배터리 충전 후) — TOP5 우수 업체 분석 + 예측 입찰가 기능 완성

- **참여업체수 구간별 차트 개선**: 값 차이가 작아(전부 -10%대) 0% 기준 막대그래프로는 추세가 안 보이던 문제를 `trendChart()`(실제 데이터 범위로 확대 + 값 라벨 직접 표시) 신규 함수로 해결. 결과: 참여업체 1~20개일 때 -10.75% → 21~50개 -10.89%(최저점) → ... → 400개 초과 -10.44%로 **U자형 패턴**이 뚜렷이 보임(참여 극소/극다 구간 모두 낙찰률이 반등하는 경향).
- **TOP5 우수 업체 분석 기능 신규 구현** (`lib/analysis.js`):
  - `getTopCompanies(n=5, {minCount=10})`: 처음엔 "낙찰건수 TOP3"로 구현했으나, 사용자가 "낙찰건수 많으면서 평균낙찰률도 높은 업체"를 요청 → **낙찰 10건 이상 업체 풀에서 건수순위+낙찰률순위 합산 종합점수(낮을수록 상위)**로 재설계, N도 3→5로 확대. 최종 TOP5: (주)청복건설(22건 89.8%) · 재흥건설(18건 90.5%) · 주식회사 금강플러스(18건 89.7%) · 동림건업(16건 89.7%) · 주식회사 신영토건(14건 89.9%).
  - **데이터 이슈 발견/수정**: "1순위업체" 필드가 애초에 `loadHistory()`에서 파싱조차 안 되고 있었음(누락) → 추가함. 또한 유찰(입찰 실패) 건이 "유찰"이라는 문자열로 1순위업체 필드에 들어있어 마치 회사처럼 집계되던 버그 발견 → `낙찰률 == null` 행 제외로 수정(예정가격=0, 낙찰금액=null인 유찰 건은 애초에 낙찰률 계산이 안 되므로 이 조건으로 자연스럽게 걸러짐).
  - `predictCompanyBid(companyName, 기초금액, 대업종)`: 특정 업체가 새 공고에 얼마로 투찰할지 예측. 예정가격 추정은 시장 전체 가중평균 예가율 사용(발주처가 정하는 값이라 업체와 무관), 투찰가는 **그 업체 본인의** 가중평균 낙찰률을 곱함. 업체의 종목별 표본이 5건 미만이면 해당 업체의 전체 낙찰이력으로 대체(신뢰도 "낮음" 표시).
- **API 추가** (`server.js`): `GET /api/top-companies`(TOP5 + 낙찰이력), `GET /api/top-companies/predictions`(진행중 공고 × TOP5 업체별 예측 입찰가).
- **대시보드 UI 추가**: "TOP5 우수 업체 분석" 섹션(카드 클릭 시 해당 업체의 전체 낙찰이력 표 확장) + "진행중 공고 × TOP5 업체 예측 입찰가" 표(가로 스크롤, 업체별 예측금액+예측낙찰률 표시, 표본부족 시 경고 표시).
- 브라우저로 전체 재검증: 콘솔 에러 0건, TOP5 카드 클릭→낙찰이력 표 정상 확장, 예측 표 가로스크롤 정상, 추세 차트 정상 확인.
- 이 세션에서 함께 처리한 참고 질문: "낙찰건수 많으면서 낙찰률도 높은 우수 업체가 있는가?" → 있음(재흥건설 등, 위 TOP5 선정 로직 자체가 이 질문에 대한 답이 되도록 설계함).

## 2026-07-05 (이어서) — 맞춤정보 리스트 추가 + TOP5 예측을 단일 상세패널로 통합

- **"진행중 공고 × TOP5 업체 예측 입찰가" 별도 표 제거, 항목별 클릭-상세패널로 통합**: 사용자가 "TOP5 예측이 진행중 입찰 항목 리스트 클릭했을 때 나오는 상세 패널 안에 표시되면 좋겠다"고 요청 → 별도 테이블 섹션을 없애고, 기존 확률4칸+안정회귀형/공격밀집형 패널 아래에 "TOP5 업체 예측 입찰가" 블록을 추가로 붙임. `server.js`의 `/api/open-bids/:postingId/analysis` 응답에 `topCompanies` 필드 추가(해당 건에 대한 TOP5 업체별 예측치를 그 자리에서 계산). `/api/top-companies/predictions` 라우트와 관련 프론트 코드(`renderPredictions`, `loadPredictions`)는 삭제.
- **"맞춤정보" 신규 스크래핑 기능 추가**: 아이건설넷 `/mybid` 페이지 — 계정에 저장된 검색조건(종목/지역 등)에 맞춰 사이트가 자동으로 매칭해주는 개인화 목록(페이지네이션 없음, 현재 33건). `/bid`와 동일한 테이블 구조라 기존 파서(`extractRowsFromPage`) 재사용. 부산/경남 지역 필터는 적용하지 않고(계정 설정을 그대로 신뢰) 경남 시/군 관내 제한 필터만 동일하게 적용.
  - `lib/scraper.js`: `scrapeMyBidList(page)` 추가, `refreshAll()`로 로그인 1회에 진행중입찰+맞춤정보 함께 갱신 후 `data/mybid_list.json`(신규, gitignore 처리)과 `data/open_bids.json`에 각각 캐시. 기존 `refreshOpenBids()` export는 하위호환을 위해 이름 유지하되 내부적으로 `refreshAll()`을 호출.
  - `server.js`: `GET /api/mybid-list` 신규 추가. `/api/open-bids/:postingId/analysis`는 이제 진행중입찰 캐시와 맞춤정보 캐시 양쪽에서 posting_id를 검색(둘 중 어디서 클릭했든 동일하게 동작).
  - 대시보드 UI: "맞춤정보 등록 항목" 섹션을 "진행중 입찰 항목 (실시간)" **바로 위**에 추가, 동일한 클릭-확장 카드 UI 재사용(`renderBidList()`로 공통화, 기존 `renderOpenBids()`를 제네릭 함수로 리팩터링). "새로고침" 버튼도 이제 두 리스트를 함께 갱신.
- 검증: `/api/mybid-list`에서 33건 정상 조회, 맞춤정보 전용 항목(예: "한국가스공사 경기지역본부" 발주, 기초금액 미공개 건)을 클릭해 상세 분석(확률4칸+전략2종+TOP5업체예측)이 한 패널에 정상 렌더링되는 것까지 스크린샷으로 확인. 콘솔 에러 0건.
- **TOP5 업체의 입찰가 vs 낙찰가(=예정가격) 차액 분석 추가**: `getTopCompanies()`에 업체별 평균예정가격/평균낙찰금액/평균차액(원)/차액범위(최소~최대) 계산 추가(원 단위 절대금액 비교, 기존 %는 이미 있었음). TOP5 카드에서 클릭 안 해도 바로 보이도록 요약 라인에 표시(예: "평균 예정가격 2.8억원 · 평균 낙찰금액 2.5억원 · 평균 차액 -2,998만원 · 차액범위 -367만원~-1.3억원"), 건별 상세표에도 "차액" 컬럼 추가(양수=초록/`--good`, 음수=빨강/`--series-6` 색상 구분). `fmtWonDiff()` 포맷터 신규 추가(부호+만원/억원 단위 자동 축약).
- **위 원단위 수치에 퍼센트(%) 병기 추가** (사용자 질문: "기초금액과 예정가격이 뭐가 다른가" → 답변 후 "그럼 몇 %로 낙찰했는지도 추가해달라"): `getTopCompanies()`에 `최소낙찰률`/`최대낙찰률` 추가(예정가격 대비 낙찰금액 비율의 최소/최대값 — 원 단위 최소/최대차액과 항상 같은 레코드끼리 대응됨, 낙찰률이 예정가격>0 곱셈이라 순서가 그대로 보존되기 때문). 카드 요약에 "평균 낙찰금액(=입찰가) 2.5억원 (예정가격 대비 89.8%)", "평균 차액 -2,998만원 (-10.16%)", "차액 범위 -367만원(-8.89%) ~ -1.3억원(-12.16%)" 형태로 표시. `fmtPctDiff()` 포맷터 신규 추가(부호 있는 %, 예: "+1.23%"/"-10.16%") — 기존에 있었지만 미사용이던 `fmtPct`를 이 이름으로 교체함.

## 2026-07-05 (이어서) — GitHub 저장소 생성 + Cloudflare Workers 정적 배포

- **GitHub 저장소 생성**: 사용자 개인 계정 `bisan74-lab` 아래 **비공개(private)** 저장소 `bid-analysis` 생성 후 전체 코드/데이터 푸시 (`gh repo create bisan74-lab/bid-analysis --private --source=. --remote=origin --push`). 커밋 전 `credentials.local.md`/`kakao.local.md`/토큰 파일이 `.gitignore`로 제외됐는지, 과거 커밋 이력에 새어나간 적 없는지 재확인함. **커밋 직전 `PROGRESS.md` 문서 안에 실제 카카오 REST API 키 일부가 평문으로 적혀있던 것을 발견해서 삭제 후 커밋** — AI가 작업 기록을 문서화할 때도 실제 시크릿 값을 그대로 옮겨적지 않도록 주의할 것(이후 시크릿은 절대 안 옮겨적기로 함). 대신 `webapp/kakao.local.md.example` 템플릿 파일(값은 비우고 형식만) 추가.
- **Cloudflare Workers 정적 배포 지원** (사용자가 GitHub-Cloudflare 연동 후 "최종 리포트(=webapp 대시보드)를 Cloudflare에 표시해달라" 요청):
  - **핵심 제약**: Cloudflare Workers는 Node.js Express 서버나 Playwright(헤드리스 브라우저)를 실행할 수 없음(서버리스 V8 격리 환경, 파일시스템/브라우저 프로세스 없음) → 실시간 스크래핑·카카오 알림이 포함된 지금의 앱을 그대로 올릴 수 없어, **빌드 시점 스냅샷을 정적 JSON으로 미리 만들어 서빙하는 방식**으로 구현. 사용자에게 이 한계와 두 후보(정적 report.html vs 정적 스냅샷 대시보드)를 먼저 확인받고, "대시보드" 쪽으로 진행하기로 결정함.
  - `webapp/data/snapshot/*.snapshot.json`: 라이브 캐시(`open_bids.json`/`mybid_list.json`, 원래 gitignore 대상)의 특정 시점 사본을 **별도 파일명으로 커밋** — Cloudflare 빌드 환경이 git 클론만으로 완결되게 하기 위함.
  - `webapp/build-static.js`: 이 스냅샷 + `lib/analysis.js`를 이용해 `/api/history/stats`, `/api/open-bids`, `/api/mybid-list`, `/api/top-companies`, 그리고 스냅샷에 있는 모든 공고 각각에 대한 `/api/analysis/<posting_id>`(추천가+전략2종+TOP5예측 전부 포함)를 정적 파일로 미리 생성해 `webapp/dist`에 출력. `public/`의 정적 자산도 함께 복사하고 `index.html`에 `window.__SNAPSHOT_MODE__`/`__SNAPSHOT_TIME__` 플래그를 주입. **의도적으로 외부 npm 패키지 의존성 0개**(fs/path/analysis.js만 사용)로 작성해서 Cloudflare 빌드 시 `npm install`(→ Playwright 브라우저 다운로드까지 트리거됨) 없이 `node webapp/build-static.js` 한 줄로 바로 실행되게 함.
  - **경로 충돌 버그 발견/수정**: 원래 API 경로 `/api/open-bids/:id/analysis`를 그대로 정적 파일로 쓰려니 `dist/api/open-bids`가 목록 응답(파일)이면서 동시에 `<id>/analysis` 하위 폴더도 있어야 해서 "파일이면서 동시에 디렉터리"인 충돌 발생 → 경로를 `/api/analysis/:id`로 분리(서버.js 라우트와 app.js fetch 양쪽 다 동일하게 수정, 로컬 서버와 정적 배포 둘 다 같은 경로 사용).
  - `wrangler.toml`(레포 루트): `[assets] directory = "./webapp/dist"` — 커스텀 워커 스크립트 없이 정적 자산만 서빙.
  - `webapp/public/vendor`(빈 폴더, 예전에 만들어놓고 안 쓴 것) 정리하며 삭제 — 안 그러면 build-static.js의 `copyFileSync`가 디렉터리를 파일처럼 취급하려다 EPERM 에러.
  - **검증**: 로컬에서 `node build-static.js` 실행 → `webapp/dist`를 간이 정적서버로 띄워 Playwright MCP 브라우저로 실제 클릭까지 확인(스냅샷 배너 표시, "정적 스냅샷 버전" 버튼 비활성 문구, 맞춤정보 항목 클릭 시 사전 생성된 분석 파일이 정상 로딩되어 확률/전략/TOP5예측 전부 표시됨). 콘솔 에러 없음(파비콘 404 제외).
  - **사용자가 Cloudflare 대시보드에서 직접 설정해야 하는 값**(AI가 대시보드 UI에 접근 불가): Build command = `node webapp/build-static.js`, Build output directory = `webapp/dist`, Root directory = 저장소 루트(기본값). npm install 스텝은 필요 없음(빌드 스크립트가 외부 의존성 없음).
  - **스냅샷 갱신 방법**(다음에 데이터를 최신화하고 싶을 때): 로컬 서버에서 "실시간 새로고침" 실행 → `webapp/data/open_bids.json`/`mybid_list.json`을 `webapp/data/snapshot/*.snapshot.json`으로 복사 → 커밋 → 푸시하면 Cloudflare가 자동 재빌드.

## 2026-07-19

- **아이건설넷 로그인 시 사이트가 자동화를 감지, 경고 팝업 발생 → 사용자 지시로 즉시 전면 중단.** 작업 스케줄러 태스크 비활성화, 서버/클라이언트/알림 스크립트의 로그인 진입점을 전부 no-op으로 막음(커밋 `54cfae5`). 상세는 `PROJECT_STATE.md` 5.5장.
- 대체 데이터 소스로 나라장터(g2b.go.kr) OpenAPI 채택. 사용자가 data.go.kr 인증키를 이미 보유 중 — `webapp/g2b.local.md`에 보관.
- **API 호출 디버깅**: 처음엔 curl로 호출했더니 계속 `403 Forbidden`(일반 OpenAPI 에러 형식이 아닌 평문). 일반 인터넷 접속(구글, data.go.kr 홈)은 정상이라 네트워크 문제는 아님을 확인. 사용자가 같은 URL을 본인 브라우저에 붙여넣었더니 정상 JSON 응답 → 브라우저 vs curl 차이로 특정. Node 내장 `fetch`로 재시도했더니 정상(200+JSON) — **curl만 WAF에 막히고 Node fetch는 통과**하는 것으로 결론. 이후 전부 fetch로 구현.
- `webapp/lib/g2b.js` 신규 작성: 최근 14일 전국 공사공고를 페이지네이션으로 받아 부산/경남 지역 + 경남 시/군 관내제한 제외(`localFilter.js` 재사용) + 공고명 키워드로 우리 업종(포장/토공/상.하수도 등) 매칭 + 마감 임박분 제외. 키워드 규칙은 `data/raw` CSV의 실제 종목 태그 빈도를 먼저 집계해서(포장 2513건이 최다, 이하 상.하수도/토공/보링.그라우팅.파일/철콘/토건/토목 순) 정함.
- **카테고리 편차 반영**: CSV의 `종목` 컬럼(대업종보다 세분화된 태그, 로딩만 되고 그동안 한 번도 안 쓰였음)을 처음 활용. `recommendBid()`에 `{종목, 발주처}` 옵션을 추가해서, 세부 종목/발주처 단위로 낙찰률이 대업종 평균과 얼마나 다른지(표본 15건/8건 이상일 때만) 계산해 tiers/전략밴드에 반영하고 `appliedAdjustments`로 근거를 노출. 3번째 인자 생략 시 기존과 동일하게 동작함을 확인(하위호환).
- **맞춤정보 재정의**: 로그인 계정 저장검색이 없어졌으므로 "기초금액이 해당 업종 시공능력평가액 이하(=실제 수주 가능 규모)"로 재정의.
- **분석 새 탭**: `analysis-render.js`(공용 렌더 모듈)로 `app.js`의 렌더 함수를 분리하고, `analysis.html`/`analysis.js`(신규)를 추가. "진행중 입찰" 클릭 시 `window.open`으로 새 탭 오픈(사용자 결정 — "맞춤정보"는 인라인 유지, 자동 새로고침은 이번엔 안 함, 수동 버튼만).
- **실측 검증**: `g2b.refreshAndCache()` 실제 호출 → 8건(전부 포장 또는 상.하수도로 정확히 분류, 부산/경남만). 로컬 서버 구동 후 Playwright로 로그인 → 진행중 입찰 카드 클릭 → 새 탭에서 추천가/전략밴드/**반영된 카테고리 편차**(예: 김해국토관리사무소 발주 건에서 종목 "포장" -0.05%p, 해당 발주처 +0.11%p, 표본 113건)/TOP5예측 전부 정상 표시 확인. `build-static.js` 재실행해서 `dist/analysis.html` 및 `appliedAdjustments` 필드 포함 확인.
- 테스트 중 로그인 페이지에서 처음 보는 ID로 자동생성된 테스트용 admin 계정(비밀번호 변경 강제됨)이 생겼길래, 테스트 종료 후 `data/admins.json`/`data/session-secret` 삭제해서 정리(둘 다 없으면 자동 재생성되는 파일이라 안전).
- **다음에 할 일**: Cloudflare 정적 배포본은 아직 옛 스냅샷(2026-07-04) 기준이라 나라장터 데이터가 안 보임 — 최신화하려면 로컬에서 새로고침 후 스냅샷 파일 복사→커밋→푸시. 카카오 자동알림도 아직 나라장터 기반으로 재연결 안 됨.
